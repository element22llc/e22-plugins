# Workspace (polyrepo spine host) additions — merge into the core .gitignore, never replace it

# Member checkouts — one line per manifest `path:`, anchored so `docs` does not also ignore `apps/docs`
# /frontend/
# /backend/

# Generated from spec/workspace.yml by `mise run ws:code`
*.code-workspace
