<!--
Template: workflow page for a /steer:<skill>.
Copy into docs/workflows/<skill>.md and fill in. Keep claims grounded in the
skill's SKILL.md frontmatter (description, when_to_use, argument-hint).
This file is excluded from the nav and the orphan check.
-->
# `/steer:<skill>`

One-sentence purpose, drawn from the skill's `description`.

!!! info "When to use"
    Mirror the skill's `when_to_use`.

**Argument hint:** `<copy from frontmatter, if any>`

## Flow

```mermaid
flowchart LR
    A[Start] --> B[Step] --> C[End]
```

## Modes

| Mode | What it does |
| --- | --- |
| `mode` | … |

## Rules it follows

- Link to the relevant [concept](../concepts/product-spine.md) or
  [authorization](../concepts/authorization-model.md) page.
