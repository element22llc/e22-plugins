# Routing case — see ../README.md for what this suite measures.
schema_version: "1.1"
name: routes-po-idea-to-build
tags: [routing]

context:
  # A steer-managed repo, so the ask is answerable. Runs only under --scaffold.
  scaffold_script: scaffold.sh

execution:
  prompt: |
    I'm not a developer but I want to build an app from my idea
  # 6 was too tight: a run that spends its budget reading the repo is killed with
  # "Reached maximum number of turns" BEFORE it writes an answer, and the graders
  # then score a truncated message — a zero that says nothing about routing.
  max_turns: 12
  allowed_tools: [Read, Glob, Grep, Skill]

runs: 1
