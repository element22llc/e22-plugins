# Dependencies
node_modules/
.pnpm-store/
.yarn/
__pycache__/
*.pyc
.venv/
venv/
target/

# Build artifacts
dist/
build/
.next/
out/
*.tsbuildinfo

# Environment — var NAMES go in the committed .env.example, values never
.env
.env.local
.env.*.local

# mise local overrides
.mise.local.toml

# Claude Code local settings
.claude/settings.local.json

# steer self-fault log (read by /steer:report)
.claude/steer-faults.log
.claude/steer-faults.surfaced

# Claude Code worktrees (linked checkouts; .worktreeinclude picks what gets copied in)
.claude/worktrees/

# /steer:work claim markers
spec/.work/

# IDE — shared VS Code config stays; mcp.json is shared because Copilot does not read .mcp.json
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/mcp.json
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test output
coverage/
.nyc_output/

# Terraform — never ignore .terraform.lock.hcl (committed provider pins)
*.tfstate
*.tfstate.*
.terraform/

# OS
Thumbs.db
