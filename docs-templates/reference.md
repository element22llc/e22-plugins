<!--
Template: reference / catalog page (skills, hooks, rules, contract).
Copy into docs/reference/<name>.md and fill in. Reference pages should be
generated/reconciled from source of truth where possible (see /plugin-docs).
This file is excluded from the nav and the orphan check.
-->
# <Reference title>

One-sentence scope. Note if this page is kept in sync by `/plugin-docs` and which
validator check guards it.

## <Category>

| Item | Purpose |
| --- | --- |
| `name` | … |
