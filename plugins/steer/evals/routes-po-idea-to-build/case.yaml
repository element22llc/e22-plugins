# Routing case — see ../README.md for what this suite measures.
schema_version: "1.1"
name: routes-po-idea-to-build
tags: [routing]

context:
  # A steer-managed repo, so the ask is answerable. Runs only under --scaffold.
  scaffold_script: scaffold.sh

execution:
  prompt: |
    I'm not a developer but I want to build an app from my idea
  max_turns: 6
  allowed_tools: [Read, Glob, Grep, Skill]

runs: 1
